services:
  # ── Unified Production Web Service (Frontend + Backend Digital Twin) ─────
  - type: web
    name: resilio-city
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
